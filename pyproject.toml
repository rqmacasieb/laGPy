[build-system]
requires = ["setuptools>=42", "wheel", "versioneer"]
build-backend = "setuptools.build_meta"

[tool.versioneer]
VCS = "git"
style = "pep440"
versionfile_source = "laGPy/_version.py"
versionfile_build = "laGPy/_version.py"
tag_prefix = "v"
parentdir_prefix = "laGPy-"